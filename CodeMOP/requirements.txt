# CodeMOP — Manager Of Personas
# Core dependencies

# YAML frontmatter parsing for persona
# and rtfm.md files
python-frontmatter>=1.0.0

# YAML config file handling
pyyaml>=6.0.0

# Ollama API and connector HTTP calls
requests>=2.31.0
httpx>=0.25.0

# HTML session file parsing
# for OpenWebUI and Pi-Coder connectors
beautifulsoup4>=4.12.0

# Filesystem watcher for session indexer
watchdog>=3.0.0

# Cron job management for cleaner
python-crontab>=3.0.0
